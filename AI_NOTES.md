# AI Notes

This file records AI-assisted proposals and the human review applied to each material decision. Each row maps a key decision to the actual code, documentation, and commit history.

| Decision                                                   | What AI first proposed                                                                                                 | What I changed / kept / rejected                                                                                                                                                                                                                                                                              | Why                                                                                                                                                                                                                                                             | Where in the code                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PostgreSQL schema for order event tracking                 | AI proposed separating order state, idempotency records, status history, pending events, and dead-letter events.       | Kept the five-table design. Kept status constraints, indexes, table comments, and transactional initialization. No Go behavior was added in this commit.                                                                                                                                                      | This schema separates current state, duplicate detection, audit history, retryable out-of-order events, and unrecoverable failures. It also avoids coupling every received event to an already-created order.                                                   | `migrations/001_init.sql`<br>`docs/adr/006-schema-design.md`<br>Code/schema commit: `7ee9f30`<br>Docs mapping commit: `9b7abe3`                                                                                                                                                                                                                                             |
| Order status state machine and out-of-order classification | AI proposed a domain-level state machine instead of directly updating the order status in the database.                | Kept the explicit state machine and required separate classifications: `APPLIED`, `DUPLICATE_STATUS`, `OUT_OF_ORDER`, and `INVALID_TRANSITION`.                                                                                                                                                               | A dedicated state machine prevents blind status overwrites, separates recoverable out-of-order events from invalid transitions, and keeps lifecycle behavior testable.                                                                                          | `internal/domain/status.go`<br>`internal/domain/state_machine.go`<br>`internal/domain/state_machine_test.go`<br>Commit: `dd749c7`                                                                                                                                                                                                                                           |
| Redis Streams queue selection                              | AI first proposed comparing Redis Lists and Redis Streams.                                                             | Kept Redis Streams for consumer groups, explicit `XACK` after the database commit, pending-message recovery, and stream depth/pending metrics. Rejected Redis Lists.                                                                                                                                          | With `BRPOP`, a worker crash after removal but before the database commit can lose an event unless extra reliability and recovery logic is built. Redis Streams provides the required delivery state natively.                                                  | `internal/queue/redis_stream.go`<br>`internal/queue/redis_stream_test.go`<br>`docs/adr/001-redis-streams.md`<br>`go.mod` / `go.sum`<br>Queue adapter commit: `b5a1f54`<br>Docs mapping commit: `488ce8f`                                                                                                                                                                    |
| Idempotency strategy                                       | AI first proposed a database unique constraint with transactional deduplication.                                       | Kept the unique constraint on `processed_events.event_id` and made PostgreSQL the source of truth. Rejected a Redis distributed lock as the primary idempotency mechanism.                                                                                                                                    | Correctness belongs in the same PostgreSQL transaction as the order update. After commit, a worker can safely `XACK`; a redelivery before acknowledgement becomes a harmless duplicate.                                                                         | `internal/service/processor.go`<br>`internal/repository/order_repository.go`<br>`docs/adr/002-idempotency.md`<br>Processor commit: `cf968b2`<br>Docs mapping commit: `a8eed6c`                                                                                                                                                                                              |
| Concurrency control                                        | AI first proposed transaction-level protection and discussed row-level locks.                                          | Strengthened the design with a PostgreSQL transaction-scoped advisory lock keyed by `order_id`, because a row lock cannot protect an order row that does not exist yet.                                                                                                                                       | The advisory lock serializes concurrent workers for the same order without serializing all orders. It is released automatically with the transaction and needs no Go or Redis lock.                                                                             | `internal/repository/order_repository.go`<br>`docs/adr/003-concurrency-control.md`<br>Processor commit: `cf968b2`<br>Docs mapping commit: `a8eed6c`                                                                                                                                                                                                                         |
| Failure recovery and pending message reclaim               | AI first proposed adding a recovery path for messages read but not acknowledged.                                       | Kept Redis Streams pending reclaim and required `XACK` only after a successful database commit. Kept the reclaimer on the same processor path as normal workers. Added deterministic failure simulation to prove recovery. Rejected recover-only logging because logs do not recover unacknowledged messages. | This preserves no-loss behavior when a worker crashes before `XACK`; the message remains pending and can be reclaimed, processed idempotently, and acknowledged after commit. The 100k report demonstrates this with simulated failures and recovered messages. | `internal/worker/pool.go`<br>`internal/worker/reclaimer.go`<br>`internal/queue/redis_stream.go`<br>`cmd/api/main.go`<br>`docs/adr/005-failure-recovery.md`<br>`reports/sample-run-100k.md`<br>Worker pool commit: `8f90de0`<br>Reclaimer commit: `5e4722a`<br>Runtime wiring commit: `ffafec4`<br>Failure simulation commit: `8927c3d`<br>Recovery report commit: `f993752` |
| Load producer design                                       | AI proposed a configurable producer that can generate high-volume order events with duplicates and out-of-order cases. | Kept deterministic generation with a seed, configurable ratios, duplicate `event_id` injection, recoverable out-of-order events, invalid transition events, and a printed producer summary.                                                                                                                   | Deterministic generation makes the 100k report reproducible and gives reviewers concrete evidence for total events, duplicate count, out-of-order count, invalid transition count, and unique order count.                                                      | `cmd/producer/main.go`<br>`cmd/producer/generator.go`<br>`cmd/producer/generator_test.go`<br>`README.md`<br>Producer commit: `e0df4ab`                                                                                                                                                                                                                                      |
| Live monitoring and stats semantics                        | AI proposed live runtime metrics for processed events, duplicates, failures, and queue depth.                          | Kept `/stats`, then clarified metric semantics so `processed` counts successful handling attempts and `stream_length`, `queue_depth`, and `pending_depth` are reported separately.                                                                                                                            | The reviewer needs to see whether work is complete, pending, or retained in Redis history. Separating backlog, pending entries, and stream length prevents misleading queue interpretation after messages are acknowledged.                                     | `cmd/api/main.go`<br>`internal/monitoring/metrics.go`<br>`internal/monitoring/metrics_test.go`<br>`internal/queue/redis_stream.go`<br>`internal/worker/pool.go`<br>Stats endpoint commit: `8adc32f`<br>Stats semantics fix commit: `952305a`                                                                                                                                |
